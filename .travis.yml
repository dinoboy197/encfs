language: cpp

sudo: false

compiler:
  - clang

before_script:
  - mkdir build
  - cd build
  - cmake ..

script:
  - make && ./checkops
  - cd ..
  - ./test.sh

addons:

  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - clang
      - cmake
      - libfuse-dev

